name: ai-dashboard
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.13
  - pip
  - pip:
      - Django>=6.0,<6.1
      - psutil>=7.2
      - nvidia-ml-py>=12.0
      - django-allauth[socialaccount]>=65,<66
      - gunicorn>=21.0
      - whitenoise[brotli]>=6.7
